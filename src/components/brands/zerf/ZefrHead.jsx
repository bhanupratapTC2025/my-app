const ZefrHead = () => {
  return (
    <section className="w-full bg-gray-100 py-10">
      <div className="max-w-screen-xl mx-auto px-4">
        
        <p className="text-lg text-gray-700 text-center">
          This section spans the full width of the screen. The text is centered within a container, but you can adjust it
          to stretch edge-to-edge if desired. Tailwind's utility classes make it easy to style sections like this.
        </p>
      </div>
    </section>
  );
};

export default ZefrHead;
